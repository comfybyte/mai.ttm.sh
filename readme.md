Source code for my tilde.team page. Live at https://ttm.sh/~mai/.

## Developing
For now it's dead simple, just edit `./src`.

A Nix shell with `live-server` and `just` is available. `just watch` to spin up live reload.
